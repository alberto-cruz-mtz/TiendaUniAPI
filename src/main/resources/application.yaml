spring:
  profiles:
    active: dev
    default: ${PROFILE:dev}
  mvc:
    problemdetails:
      enabled: true
  application:
    name: TiendaUniAPI
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false